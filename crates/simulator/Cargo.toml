[package]
name    = "simulator"
version = "0.1.0"
edition = {workspace = true}

[lib]
path = "lib.rs"

[dependencies]
domain    = { path = "../domain" }
thiserror = {workspace = true}

[lints.rust]
unsafe_code = "forbid"
